version: "3.8"

services:
  app:
    stdin_open: true
    container_name: nextjs-app-router-boilerplate-dev
    image: nextjs-app-router-boilerplate-dev
    ports:
      - "3000:3000"
    build:
      context: .
      dockerfile: "Dockerfile.dev"
